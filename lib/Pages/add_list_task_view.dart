import 'package:flutter/material.dart';

class AddListTask extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add List Task")),
    );
  }
}
